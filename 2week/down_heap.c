#include <stdio.h>
#define SWAP(a,b,t) ((t=a),(a=b),(b=t))

#define N 10
int a[N], b[N], n;

void input()
{
    scanf("%d", &n);
    int t;
    for(int i = 1; i <= N; i++){
        scanf("%d", &a[i]);
        while(j == 1 && )
        if(a[i] > a[i/2]){
            SWAP(a[i], a[i/2], t)
        }
    }
}
void run()
{
    int i;
    for(int i = 1; i <=n; i++)
}
int main()
{
    freopen("/Users/eugene/eugene-lab/algorithm-cpp/2week/input.txt", "r", stdin);
    input();

    int heaptree[10];



    return 0;
}